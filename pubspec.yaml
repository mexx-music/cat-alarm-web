name: cat_alarm

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.39
  audio_session: ^0.1.16

dev_dependencies:
  flutter_test:
    sdk: flutter
  import_lint: ^1.0.1   # <-- statt ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
